# Post-#80 pickup handoff

**Date:** 2026-04-24
**Supersedes:** `docs/superpowers/reviews/2026-04-21-session-handoff.md` for
"What's next" — that doc's *Next* section is now done. Everything else it
says about scope, rationale, and sandbox gotchas still applies.

## Where we are

- **Fix-now phase is done.** All 20 Fix-now findings and 36 clarity
  suggestions from the 2026-04-22 correctness review are merged. Landing
  page is polished (hero copy, buttons, links, code panels, asides, hero
  image, growth-path diagram rebuilt).
- **Pre-merge cleanup is done.** PR #77 (a11y, stylesheet fixes, content-
  correctness regression-guard test, BOM strip, growth-path rebuild) is
  merged into `feature/cyoda-go-init`.
- **PR #76 is open and mergeable** (`feature/cyoda-go-init` → `main`).
  Title: *feat(docs): integrate cyoda-go-init*. Surge preview live at
  https://cyoda-docs-feature-cyoda-go-init.surge.sh/. Merge when ready;
  no further fixes pending on it.

## What's deferred until cyoda-go #80 lands

[cyoda-go #80 — *Ship a topic-structured `cyoda help` surface*](https://github.com/Cyoda-platform/cyoda-go/issues/80)
is the upstream dependency. Full proposal and implications for this repo are
in `docs/superpowers/reviews/2026-04-21-upstream-issues.md` §"Issue 9" and
§"Issue 11".

The filed cyoda-docs follow-up is
[**cyoda-docs #69**](https://github.com/Cyoda-platform/cyoda-docs/issues/69).
That issue is the single pickup anchor when #80 ships — everything below is
a rollup of what it implies for this repo.

### Concrete work items (post-#80)

1. **Build-pipeline import of help assets.** Fetch `help.tar.gz` from the
   pinned cyoda-go release, extract under `src/content/vendored/help/**`,
   gitignore-per-build (mirror the existing schema-pages generator
   pattern). See `scripts/generate-schema-pages.js` for the template.
2. **Reframe the three `awaiting-upstream` stub pages** as navigator/
   concept pages and drop their banners:
   - `src/content/docs/reference/cli.mdx`
   - `src/content/docs/reference/configuration.mdx`
   - `src/content/docs/reference/helm.mdx`
3. **"From the binary" callout** on every page that maps to a help topic
   (most of `build/`, `run/`, and parts of `concepts/`). Pattern:
   > Canonical reference: `cyoda help <topic>`. This page is the
   > narrative; the binary is authoritative.
4. **Dispatch the 5 Reframe-post-#80 findings and 1 Delete-post-#80
   finding** from the correctness review. The items are enumerated in
   `docs/superpowers/reviews/2026-04-22-correctness/ledger.md` and the
   per-page reviews under `.../pages/*.md` — grep for `Reframe post-#80`
   and `Delete post-#80` to find them.
5. **Run the Post-#80 alignment test** per
   `docs/superpowers/specs/2026-04-22-cyoda-docs-correctness-review-design.md`
   §"Post-#80 alignment test": ledger ↔ `help.json` diff per surface
   (green/amber/red), then per-page Reframe/Delete dispatch against the
   11 pre-enumerated items.
6. **Drop `VendoredBanner`'s `awaiting-upstream` stability mode** once the
   three pages above no longer use it. `upcoming` (Trino) stays until
   Trino ships.

### Pinned cyoda-go reference

The correctness ledger was built against cyoda-go
`6442de4696854ee8aa3b6d2ea9345b9c96eb6aad`
(`docs/superpowers/reviews/2026-04-22-correctness/CYODA_GO_SHA`). When
#80 lands, re-run the alignment test against the new SHA; anything that
diverges becomes a Fix-now finding for the post-#80 PR.

## Also open and unrelated to #80

- [cyoda-docs #68](https://github.com/Cyoda-platform/cyoda-docs/issues/68) —
  dedicated `build/searching-entities.md` page for the REST search grammar.
  Independent of #80. Scope in upstream-issues.md §"Issue 10".
- Trino "upcoming" banner stays on four pages until Trino ships
  (`concepts/apis-and-surfaces`, `build/analytics-with-sql`,
  `build/testing-with-digital-twins`, `reference/trino`).

## Entry points for a fresh session

In decreasing priority:

1. **This file.** Current state + link map.
2. **cyoda-docs #69** (and upstream cyoda-go #80) — track the dependency.
3. `docs/superpowers/reviews/2026-04-21-upstream-issues.md` — the "why" and
   the release-asset contract each pending item depends on.
4. `docs/superpowers/reviews/2026-04-22-correctness/` — ground-truth ledger
   and per-page reviews; lookup table when applying a specific Reframe.
5. `docs/superpowers/specs/2026-04-22-cyoda-docs-correctness-review-design.md`
   §"Post-#80 alignment test" — the test procedure.
